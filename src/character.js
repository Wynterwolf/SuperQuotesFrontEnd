class Character{
    constructor(id, name){
        this.id = id;
        this.name = name;
    }

    //render character for quote
    renderCharacter(){
        let charBox = document.getElementById("character")
        character.innerHTML +=
        `${this.name}`
    }
}
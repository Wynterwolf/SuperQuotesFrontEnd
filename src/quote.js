class Quote{
    constructor(quote, id){
        this.quote = quote;
        this.id = id
    }
// render quote instance method

    renderQuote(){
        let quoteBox = document.getElementById("quote")
        quote.innerHTML +=
        `${this.quote}`
    }
}
